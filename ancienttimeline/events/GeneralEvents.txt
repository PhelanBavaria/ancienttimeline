namespace = gen

province_event = {

	id = gen.1
	hidden = yes

	trigger = {
		owner = { is_at_war = yes }
	}
	mean_time_to_happen = {
		months = 0.01
	}

	controller = {
		add_core = ROOT
		ROOT = { cede_province = PREV }
	}
}